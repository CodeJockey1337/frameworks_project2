<p id="notice"><%= notice %></p>

<h1>Students Enrolled at MSUD</h1>

<table>
  <thead>
    <tr>
      <th>Full Name</th>
      <th>Student ID Number</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.student_name %></td>
        <td><%= student.student_number %></td>
        <td><%= link_to 'View student details', student %></td>
        <td><%= link_to 'Edit student details and add class sections', edit_student_path(student) %></td>
        <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Student', new_student_path %>


<p>
    Click here for <%= link_to "Professors", professors_url %> page <br>
    Click here for <%= link_to "Sections", sections_url %> page <br>
    Click here for <%= link_to "Courses", courses_url %> page <br>
</p>
<p>
  Click here for <%= link_to "Home", root_url %> page <br>
</p>